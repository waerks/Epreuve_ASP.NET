@model IEnumerable<UI.Models.Utilisateur.UtilisateurListItem>

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
	<a asp-action="Create">Create New</a>
</p>

<table>
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Email)
			</th>
						<th>
				@Html.DisplayNameFor(model => model.Pseudo)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Email)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Pseudo)
				</td>
				<td>
					<div class="btn-group">
						@Html.ActionLink("Edit", "Edit", new { id = item.UtilisateurId }, new { @class = "btn btn-secondary" })
						@Html.ActionLink("Details", "Details", new { id = item.UtilisateurId }, new { @class = "btn btn-primary" })
						@Html.ActionLink("Delete", "Delete", new { id = item.UtilisateurId }, new { @class = "btn btn-danger" })

					 </div>
				</td>
			</tr>
		}
	</tbody>
</table>